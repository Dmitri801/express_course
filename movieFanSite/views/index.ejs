<!DOCTYPE html>
<html>
<head>
	<title>	Movie App </title>
	<% include head %>
</head>
<body>

	<% include navbar %>

	<div class="container">
		<div class="row">
			<div class="col-sm-12">
				<h1 class="text-center">Now Playing</h1>
			</div>
			<div class="col-sm-12">
					<% for(let i = 0; i < nowPlayingData.length; i++) { %>
					
						<div class="col-md-3 col-sm-4 poster">
					  <a href="/movie/<%= nowPlayingData[i].id %>">	<img src="<%= imageBaseUrl %><%= nowPlayingData[i].poster_path %>" alt="<%= nowPlayingData[i].title %>" > </a>
						</div>
 <%  } %>				
			</div>
		</div>
	</div>
</body>
</html>
